<div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
<div class="mt-5">
    <a href="{{ route('template.builder.originate') }}">
        <button type="submit" class="button w-50 bg-theme-1 text-white" name="editor" value="classic">Create Email Template</button>
    </a>
    <a href="{{ route('templates.index') }}">
        <button type="submit" class="button w-50 bg-theme-1 text-white" name="editor" value="pro">Email Template List</button>
    </a>
</div>
</div>